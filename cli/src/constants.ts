/** Shared constants for the Wani CLI. */

export const CLI_NAME = "wani";
export const CLI_VERSION = "0.1.0";

/** Official Wani Developer API base URL. */
export const DEFAULT_BASE_URL = "https://developers.aiwni.com";

/** Developer Portal URL (docs, API keys page). */
export const PORTAL_URL = "https://developers.aiwni.com";

/** Default per-request timeout in milliseconds. */
export const DEFAULT_TIMEOUT_MS = 15_000;

/** Env var holding a project API key (`wani_live_...`). */
export const API_KEY_ENV_VAR = "WANI_API_KEY";

/** Env var overriding the API base URL. */
export const BASE_URL_ENV_VAR = "WANI_BASE_URL";
